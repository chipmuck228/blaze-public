# Coach Dashboard 分析报告

## 一、现有功能分析

### 1.1 Dashboard 页面 (`/coach/page.tsx`)
**当前功能：**
- ✅ 显示统计卡片：
  - Total Classes（总课程数）
  - Upcoming Classes（即将开始的课程）
  - Ongoing Classes（进行中的课程）
  - Completed Classes（已完成的课程）
- ✅ 快速操作链接：
  - "View All Classes" → `/coach/classes`
  - "View Schedule" → `/coach/schedule`

**存在的问题：**
- ❌ 只显示数字统计，没有具体的课程列表
- ❌ 无法快速查看即将开始的课程详情
- ❌ 缺少最近课程的时间线视图

### 1.2 My Classes 页面 (`/coach/classes/page.tsx`)
**当前功能：**
- ✅ 显示所有分配的课程实例
- ✅ 支持搜索和状态过滤
- ✅ 卡片式布局，显示课程基本信息
- ✅ 点击可查看详情

**优点：**
- 信息完整，包含课程名称、日期、时间、地点、学生数量
- 支持过滤和搜索

### 1.3 Schedule 页面 (`/coach/schedule/page.tsx`)
**当前功能：**
- ✅ 日历视图显示课程
- ✅ 按月浏览
- ✅ 显示每个课程的具体日期和时间

**优点：**
- 直观的日历视图
- 可以快速查看某个月的课程安排

## 二、数据获取机制

### 2.1 API 端点
- `GET /api/coach/stats` - 获取统计数据
- `GET /api/coach/instances` - 获取所有课程实例（支持过滤）

### 2.2 数据库查询
- 通过 `course_instance_coaches` 表查询分配给该 coach 的实例
- 支持向后兼容，也检查 `instructor_id` 字段
- 使用 `getCoachInstances()` 函数获取数据

## 三、优化建议

### 3.1 Dashboard 增强
**建议添加：**
1. **即将开始的课程列表**（最近 5-10 个）
   - 显示课程名称、日期、时间、地点
   - 按开始日期排序
   - 点击可查看详情

2. **今日课程提醒**
   - 如果今天有课程，显示醒目的提醒卡片
   - 显示课程时间和地点

3. **最近完成的课程**
   - 显示最近完成的 3-5 个课程
   - 可以快速回顾

### 3.2 信息展示优化
**建议改进：**
1. **课程状态可视化**
   - 使用不同颜色的 Badge 区分状态
   - 添加进度指示器（对于进行中的课程）

2. **快速操作**
   - 在 Dashboard 上添加"查看详情"按钮
   - 添加"导出日历"快捷按钮

3. **数据分组**
   - 按状态分组显示课程
   - 按日期分组显示课程

## 四、实现方案

### 方案 A：增强 Dashboard（推荐）
在 Dashboard 上添加：
1. 即将开始的课程列表（最近 7 天内的课程）
2. 今日课程提醒
3. 最近完成的课程（最近 5 个）

### 方案 B：优化现有页面
1. 在 My Classes 页面添加更多过滤选项
2. 添加按日期分组的视图
3. 添加课程时间线视图

### 方案 C：混合方案
结合方案 A 和 B，既增强 Dashboard，又优化列表页面。

## 五、推荐实现

**推荐采用方案 A（增强 Dashboard）**，因为：
1. Dashboard 是教练登录后看到的第一个页面
2. 快速查看即将开始的课程对教练很重要
3. 可以在 Dashboard 上提供概览，详细列表在 My Classes 页面查看

