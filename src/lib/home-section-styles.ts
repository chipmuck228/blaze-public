/** Solid marketing hero background — same as `Hero` (`--hero-background` / `.bg-hero`). */
export const HOME_HERO_SECTION_BG = "bg-hero"

/**
 * Optional tl→br depth on top of `bg-hero` (all stops stay navy; matches hero tone).
 * Use with `HOME_HERO_SECTION_BG` on the same section.
 */
export const HOME_HERO_SECTION_GRADIENT_OVERLAY =
  "absolute inset-0 z-0 pointer-events-none bg-gradient-to-br from-[#0f172a] via-[#1e3a5f] to-[#172554]"
