# Course Status 变更影响分析

## 问题概述

如果 `suspended` 和 `archived` 课程在公开页面不可访问，那么对 course status 的修改就要很小心，要避免状态改变后，在前端用户就无法看到这个课程的详细内容，尽管该课程有显示。

## 当前实现分析

### 1. 课程显示位置

#### 首页 Featured Courses (`/api/courses/featured`)
- **状态过滤**：✅ 只返回 `status = 'published'` 的课程
- **影响**：如果课程状态改为 `suspended` 或 `archived`，会立即从首页消失

#### 课程目录 (`/api/courses` 或 `/api/programs`)
- **状态过滤**：✅ 只返回 `status = 'published'` 的课程
- **影响**：如果课程状态改为 `suspended` 或 `archived`，会立即从目录消失

#### 课程详情页 (`/course-catalog/[slug]` 或 `/api/courses/[id]`)
- **状态过滤**：✅ 只返回 `status = 'published'` 的课程
- **影响**：如果课程状态改为 `suspended` 或 `archived`，详情页返回 404

### 2. 潜在问题场景

#### 场景 1：用户正在浏览课程详情页
**情况**：
- 用户打开了课程详情页（`/course-catalog/intro-robotics`）
- 管理员同时将课程状态从 `published` 改为 `suspended`
- 用户刷新页面或点击链接 → 404 错误

**影响**：
- ❌ 用户体验差：突然无法访问
- ❌ 可能引起困惑：刚才还能看到，现在看不到了

#### 场景 2：课程在列表中显示，但详情页无法访问
**情况**：
- 课程在首页或目录中显示（因为之前是 `published`）
- 用户点击"Learn More"或"View Details"
- 管理员已将状态改为 `suspended` 或 `archived`
- 详情页返回 404

**影响**：
- ❌ 数据不一致：列表显示但详情无法访问
- ❌ 用户体验差：点击后无法查看详情

#### 场景 3：用户已将课程加入购物车
**情况**：
- 用户将 `published` 状态的课程加入购物车
- 管理员将课程状态改为 `suspended` 或 `archived`
- 用户尝试查看购物车中的课程详情 → 404
- 用户尝试结账 → 可能失败（如果 API 检查状态）

**影响**：
- ❌ 购物车中的课程无法查看详情
- ❌ 可能无法完成结账
- ❌ 需要清理购物车中的无效课程

#### 场景 4：用户已注册课程（enrolled）
**情况**：
- 用户已注册 `published` 状态的课程
- 管理员将课程状态改为 `suspended` 或 `archived`
- 用户尝试查看已注册课程的详情 → 404

**影响**：
- ❌ 用户无法查看已注册课程的详情
- ❌ 可能影响用户对课程的理解和准备

#### 场景 5：课程有活跃的 Instances
**情况**：
- 课程有多个活跃的 instances（正在进行或即将开始）
- 管理员将课程状态改为 `suspended` 或 `archived`
- 这些 instances 仍然存在，但课程详情无法访问

**影响**：
- ❌ 已注册的学生无法查看课程详情
- ❌ 教练无法查看课程详情（如果 Coach Portal 也过滤状态）
- ❌ 数据不一致：instances 存在但课程不可访问

## 问题根源

### 1. 状态变更的即时性
- 状态变更立即生效
- 没有"过渡期"或"警告期"
- 前端缓存可能导致数据不一致

### 2. 数据一致性问题
- 课程列表和详情页的状态过滤可能不同步
- 缓存的数据可能显示过时的状态
- 用户操作（加入购物车、注册）和状态变更可能冲突

### 3. 缺少状态变更前的检查
- 没有检查是否有活跃的 instances
- 没有检查是否有 pending enrollments
- 没有检查是否有用户正在查看该课程

## 解决方案讨论

### 方案 A：状态变更前检查（推荐）

#### 1. 检查活跃的 Instances
在将课程状态改为 `suspended` 或 `archived` 前，检查：
- 是否有 `scheduled` 或 `ongoing` 状态的 instances
- 如果有，阻止状态变更或显示警告

#### 2. 检查活跃的 Enrollments
检查：
- 是否有 `cart`、`reserved`、`enrolled` 状态的 enrollments
- 如果有，阻止状态变更或显示警告

#### 3. 状态变更确认对话框
在 Admin UI 中，当尝试将 `published` 改为 `suspended` 或 `archived` 时：
- 显示确认对话框
- 列出潜在影响：
  - "X 个活跃的 instances 将受到影响"
  - "X 个用户已注册此课程"
  - "此课程将从公开页面移除"
- 要求管理员确认

#### 4. 渐进式状态变更
- `published` → `suspended`：先检查，允许变更，但显示警告
- `published` → `archived`：更严格的检查，可能需要先处理所有活跃的 instances

### 方案 B：允许已注册用户访问（部分访问）

#### 1. 已注册用户例外
- 如果用户已注册该课程（`enrolled` 状态），允许访问课程详情
- 即使课程状态是 `suspended` 或 `archived`

#### 2. 购物车中的课程例外
- 如果课程在用户的购物车中，允许访问课程详情
- 但显示警告："此课程已暂停/归档，可能无法注册"

#### 3. 实现方式
修改 `/api/courses/[id]/route.ts`：
```typescript
// 检查用户是否已注册此课程
const enrollment = await getUserEnrollment(userId, courseId)
if (enrollment && enrollment.status === 'enrolled') {
  // 允许访问，即使状态不是 published
  return course
}
```

### 方案 C：状态变更通知和清理

#### 1. 状态变更后清理
- 自动清理购物车中的无效课程
- 通知已注册用户课程状态变更
- 提供替代方案（如果有）

#### 2. 后台任务
- 定期检查购物车中的课程状态
- 自动移除 `suspended` 或 `archived` 的课程
- 发送通知给用户

### 方案 D：软删除 vs 硬删除

#### 1. 状态变更的语义
- `suspended`：临时下架，但已注册用户仍可访问
- `archived`：永久归档，但已注册用户仍可访问（历史记录）

#### 2. 访问控制策略
- **公开访问**：只允许 `published` 状态
- **已注册用户**：允许访问 `published`、`suspended`、`archived`（如果已注册）
- **购物车中的课程**：允许访问 `published`、`suspended`（显示警告）

## 推荐方案：混合方案

### 1. 状态变更前检查（必须）
- ✅ 检查活跃的 instances
- ✅ 检查活跃的 enrollments
- ✅ 显示确认对话框和警告

### 2. 已注册用户例外（推荐）
- ✅ 已注册用户（`enrolled`）可以访问 `suspended` 或 `archived` 的课程详情
- ✅ 显示状态提示："此课程已暂停/归档，但您已注册，可以继续查看详情"

### 3. 购物车清理（推荐）
- ✅ 状态变更后，自动清理购物车中的无效课程
- ✅ 发送通知给用户

### 4. 状态变更日志（可选）
- ✅ 记录状态变更历史
- ✅ 记录变更原因
- ✅ 便于审计和回滚

## 实施优先级

### Phase 1：立即实施（必须）
1. **状态变更前检查**
   - 检查活跃的 instances
   - 检查活跃的 enrollments
   - 显示确认对话框

### Phase 2：短期优化（推荐）
2. **已注册用户例外**
   - 允许已注册用户访问 `suspended` 或 `archived` 的课程详情
   - 显示状态提示

### Phase 3：中期优化（可选）
3. **购物车清理**
   - 自动清理无效课程
   - 发送通知

4. **状态变更日志**
   - 记录变更历史
   - 审计功能

## 最佳实践建议

### 1. 状态变更工作流
```
管理员尝试变更状态
  ↓
检查是否有活跃的 instances/enrollments
  ↓
如果有，显示警告和确认对话框
  ↓
管理员确认后执行变更
  ↓
（可选）发送通知给受影响用户
  ↓
（可选）清理购物车中的无效课程
```

### 2. 访问控制策略
- **公开访问**：只允许 `published`
- **已注册用户**：允许访问 `published`、`suspended`、`archived`（如果已注册）
- **购物车中的课程**：允许访问 `published`、`suspended`（显示警告，可能无法结账）

### 3. 状态变更建议
- **`published` → `suspended`**：
  - 检查活跃的 instances
  - 如果有，建议先处理 instances（完成或取消）
  - 或者允许变更，但已注册用户仍可访问
  
- **`published` → `archived`**：
  - 更严格的检查
  - 建议所有 instances 都已完成或取消
  - 或者允许变更，但已注册用户仍可访问（历史记录）

### 4. 用户体验优化
- 在课程详情页显示状态提示
- 如果课程是 `suspended`，显示："此课程已暂停，暂时不接受新注册"
- 如果课程是 `archived`，显示："此课程已归档，不再提供"

## 总结

### 核心问题
1. **数据一致性**：列表显示但详情无法访问
2. **用户体验**：突然无法访问已查看的课程
3. **业务逻辑**：已注册用户无法查看课程详情

### 推荐解决方案
1. **状态变更前检查**（必须）
2. **已注册用户例外**（推荐）
3. **购物车清理**（推荐）
4. **状态变更日志**（可选）

### 关键原则
- **最小化影响**：状态变更不应影响已注册用户
- **数据一致性**：确保列表和详情页的状态一致
- **用户体验**：提供清晰的提示和替代方案
- **业务连续性**：已注册的课程应该可以继续访问

