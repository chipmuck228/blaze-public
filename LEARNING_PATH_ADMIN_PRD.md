# Learning Path System - Admin Portal PRD

## 文档信息
- **产品名称**: Learning Path Management System (Admin Portal)
- **版本**: 1.0
- **创建日期**: 2025-01-XX
- **最后更新**: 2025-01-XX
- **状态**: Phase 1-4 已完成

---

## 1. 产品概述

### 1.1 产品定位
为管理员提供完整的学习路径和先修课程管理系统，支持创建、编辑、管理课程依赖关系和学习路径，确保课程体系的逻辑性和完整性。

### 1.2 核心价值
- **结构化课程管理**: 通过先修课程关系建立清晰的课程依赖体系
- **学习路径规划**: 创建完整的学习序列，指导学员的学习路径
- **数据驱动决策**: 通过分析报告了解课程和路径的使用情况
- **灵活的先修逻辑**: 支持简单的 AND 关系和复杂的 OR 关系（通过组）

---

## 2. 功能模块

### 2.1 先修课程管理 (Phase 1 & 3)

#### 2.1.1 功能描述
管理员可以在课程编辑页面管理每门课程的先修课程要求。

#### 2.1.2 核心功能
- **添加先修课程**
  - 选择先修课程（从已发布的课程列表）
  - 设置要求类型：`required`（必填）、`recommended`（推荐）、`optional`（可选）
  - 设置是否强制（`is_mandatory`）
  - 设置显示顺序
  - 添加备注说明

- **编辑先修课程**
  - 修改要求类型
  - 调整显示顺序
  - 更新备注

- **删除先修课程**
  - 删除单个先修关系
  - 批量删除

- **先修课程组管理** (Phase 3)
  - 创建先修课程组（AND/OR 关系）
  - 设置组类型：
    - `and`: 组内所有课程都必须完成
    - `or`: 组内至少完成 `min_required` 门课程
  - 添加课程到组
  - 设置组描述（如"完成以下任意一门课程"）
  - 删除组

#### 2.1.3 业务规则
- 防止循环依赖：A → B → C → A（系统自动检测）
- 防止自引用：课程不能将自己设为先修课程
- 只有 `published` 状态的课程可以作为先修课程
- 只有 `published` 状态的课程可以被分配先修课程

#### 2.1.4 UI/UX 要求
- 在课程编辑对话框中添加 "Prerequisites" 标签页
- 显示先修课程列表（表格形式）
- 支持拖拽排序
- 显示先修课程组（可展开/折叠）
- 提供添加/编辑/删除按钮

---

### 2.2 学习路径管理 (Phase 2)

#### 2.2.1 功能描述
管理员可以创建和管理完整的学习路径，定义课程的学习顺序和阶段。

#### 2.2.2 核心功能
- **创建学习路径**
  - 路径基本信息：
    - 名称（必填）
    - Slug（URL 友好标识符，可选）
    - 描述
    - 关联的课程类别
    - 目标受众
    - 预计完成时间（周）
    - 难度级别：`beginner`、`intermediate`、`advanced`
    - 是否激活（`is_active`）
    - 显示顺序

- **管理路径课程**
  - 添加课程到路径
  - 设置课程阶段（Stage 1, 2, 3...）
  - 设置阶段名称（如"基础阶段"、"进阶阶段"）
  - 设置课程属性：
    - 是否必填（`is_required`）
    - 是否可以并行学习（`is_parallel`）
    - 预计完成时间（周）
    - 备注
  - 调整课程顺序（在阶段内和阶段间）
  - 删除课程

- **编辑学习路径**
  - 修改路径基本信息
  - 重新组织课程顺序
  - 添加/删除课程
  - 修改阶段名称

- **删除学习路径**
  - 删除路径及其所有课程关联
  - 确认对话框（防止误删）

#### 2.2.3 业务规则
- 路径名称必须唯一（在同一类别内）
- 只有 `published` 状态的课程可以添加到路径
- 路径必须至少包含一门课程
- 删除路径不会影响课程本身

#### 2.2.4 UI/UX 要求
- 学习路径管理页面（列表视图）
  - 显示所有路径
  - 搜索和过滤（按类别、难度、状态）
  - 添加/编辑/删除按钮
- 路径编辑对话框
  - 两个标签页：基本信息、课程管理
  - 按阶段分组显示课程
  - 支持拖拽排序
  - 实时预览路径结构

---

### 2.3 用户完成记录管理 (Phase 3)

#### 2.3.1 功能描述
管理员和教练可以手动创建用户的课程完成记录，用于跟踪学习进度。

#### 2.3.2 核心功能
- **创建完成记录**
  - 选择用户
  - 选择课程
  - 选择具体实例（可选）
  - 设置完成日期
  - 设置成绩/等级（可选）
  - 上传证书（可选）
  - 添加备注
  - 自动标记为已验证（由创建者验证）

- **查看完成记录**
  - 查看用户的所有完成记录
  - 按课程、日期筛选
  - 查看证书链接

- **编辑完成记录**
  - 修改完成日期
  - 更新成绩
  - 更新证书链接
  - 修改备注

- **删除完成记录**
  - 删除错误的记录
  - 确认对话框

#### 2.3.3 业务规则
- 只有 `admin` 和 `coach` 角色可以创建完成记录
- 完成记录会自动更新学习路径进度
- 删除完成记录会影响路径进度计算

---

### 2.4 数据分析与报告 (Phase 4)

#### 2.4.1 功能描述
管理员可以查看课程和路径的使用情况、用户学习统计等分析数据。

#### 2.4.2 核心功能
- **课程分析**
  - 课程注册人数
  - 课程完成率
  - 先修课程满足情况
  - 课程在路径中的使用情况

- **路径分析**
  - 路径注册人数
  - 路径完成率
  - 平均完成时间
  - 各阶段完成情况

- **用户学习统计**
  - 总完成课程数
  - 总注册课程数
  - 学习路径参与情况
  - 学习强度（最近30天活动）

#### 2.4.3 UI/UX 要求
- 在 Admin Dashboard 添加分析卡片
- 提供图表可视化（柱状图、折线图）
- 支持导出报告（CSV/PDF）

---

## 3. 技术实现

### 3.1 数据库表
- `course_prerequisites` - 先修课程关系
- `prerequisite_groups` - 先修课程组
- `prerequisite_group_items` - 组内课程
- `learning_paths` - 学习路径
- `learning_path_courses` - 路径课程关联
- `user_course_completions` - 用户完成记录
- `user_learning_path_progress` - 用户路径进度

### 3.2 API 端点
- `GET /api/admin/courses/[id]/prerequisites` - 获取先修课程
- `POST /api/admin/courses/[id]/prerequisites` - 添加先修课程
- `PUT /api/admin/courses/[id]/prerequisites/[prereqId]` - 更新先修课程
- `DELETE /api/admin/courses/[id]/prerequisites/[prereqId]` - 删除先修课程
- `GET /api/admin/courses/[id]/prerequisite-groups` - 获取先修课程组
- `POST /api/admin/courses/[id]/prerequisite-groups` - 创建组
- `DELETE /api/admin/courses/[id]/prerequisite-groups/[groupId]` - 删除组
- `GET /api/admin/learning-paths` - 获取所有路径
- `POST /api/admin/learning-paths` - 创建路径
- `GET /api/admin/learning-paths/[id]` - 获取单个路径
- `PUT /api/admin/learning-paths/[id]` - 更新路径
- `DELETE /api/admin/learning-paths/[id]` - 删除路径
- `POST /api/user/completions` - 创建完成记录（admin/coach）

### 3.3 权限控制
- 所有管理功能需要 `admin` 角色
- 创建完成记录需要 `admin` 或 `coach` 角色
- 使用 NextAuth.js 进行身份验证和授权

---

## 4. 用户体验要求

### 4.1 界面设计
- 使用统一的 Admin Portal 设计语言
- 响应式设计，支持桌面和移动设备
- 清晰的导航和面包屑
- 一致的按钮和表单样式

### 4.2 交互设计
- 实时验证和错误提示
- 加载状态指示
- 确认对话框（删除操作）
- 成功/失败消息提示

### 4.3 性能要求
- 列表加载时间 < 2 秒
- 搜索响应时间 < 500ms
- 支持分页（每页 20-50 项）

---

## 5. 测试要求

### 5.1 功能测试
- 先修课程 CRUD 操作
- 先修课程组创建和管理
- 学习路径 CRUD 操作
- 完成记录创建和管理
- 循环依赖检测
- 权限验证

### 5.2 集成测试
- API 端点测试
- 数据库操作测试
- 前端组件测试

### 5.3 性能测试
- 大量数据下的列表加载
- 复杂查询性能
- 并发操作测试

---

## 6. 未来扩展

### 6.1 短期（1-3个月）
- 批量导入先修课程关系
- 路径模板功能
- 更详细的分析报告

### 6.2 中期（3-6个月）
- 路径版本管理
- A/B 测试不同路径效果
- 自动化路径推荐

### 6.3 长期（6-12个月）
- AI 驱动的路径优化
- 个性化路径生成
- 多语言支持

---

## 7. 成功指标

### 7.1 使用指标
- 管理员创建路径数量
- 先修课程关系覆盖率
- 完成记录创建数量

### 7.2 质量指标
- 系统错误率 < 1%
- 用户满意度 > 4.5/5
- 功能使用率 > 80%

---

## 8. 附录

### 8.1 术语表
- **先修课程 (Prerequisite)**: 学习某门课程前需要完成的课程
- **学习路径 (Learning Path)**: 一系列按顺序组织的课程
- **阶段 (Stage)**: 学习路径中的学习阶段
- **完成记录 (Completion)**: 用户完成某门课程的记录

### 8.2 相关文档
- `LEARNING_PATH_DESIGN.md` - 系统设计文档
- `LEARNING_PATH_USER_PRD.md` - 用户端 PRD
- `TOP_LEVEL_DESIGN.md` - 顶层设计文档

