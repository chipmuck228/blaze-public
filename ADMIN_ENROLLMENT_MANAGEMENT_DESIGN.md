# Admin Portal - Enrollment 管理功能设计

## 1. 最佳实践分析

### 1.1 为什么需要 Enrollment 管理？

从最佳实践的角度，管理员应该能够管理所有 enrollment，无论状态如何，原因包括：

#### 1.1.1 业务需求
- **客户服务**：处理用户问题、退款请求、状态异常
- **财务对账**：查看支付记录、退款记录、收入统计
- **运营监控**：监控注册趋势、课程满员情况、等待列表长度
- **异常处理**：处理支付失败、系统错误导致的异常状态

#### 1.1.2 合规性
- **审计追踪**：完整的注册历史记录
- **数据导出**：满足财务、税务、报告需求
- **状态追溯**：查看状态变更历史

#### 1.1.3 运营效率
- **批量操作**：批量取消、批量退款
- **快速搜索**：按用户、课程、日期快速查找
- **统计分析**：注册率、完成率、取消率等

### 1.2 行业标准功能

参考电商、教育平台、活动管理系统的最佳实践，Enrollment 管理应该包括：

1. **查看和筛选**
   - 所有状态的注册记录
   - 多维度筛选（状态、用户、课程、日期范围）
   - 搜索功能（用户姓名、邮箱、课程名称）

2. **详情查看**
   - 完整的注册信息
   - 状态变更历史
   - 支付信息
   - 用户信息

3. **状态管理**
   - 手动修改状态（如退款、取消、标记完成）
   - 批量操作
   - 状态变更原因记录

4. **统计分析**
   - 按状态统计
   - 按课程统计
   - 按日期统计
   - 收入统计

5. **导出功能**
   - 导出为 CSV/Excel
   - 按筛选条件导出

---

## 2. 功能设计

### 2.1 页面结构

```
/admin/enrollments
├── 列表视图（默认）
│   ├── 筛选器（状态、用户、课程、日期范围）
│   ├── 搜索框
│   ├── 统计卡片（总数、各状态数量）
│   └── 数据表格
├── 详情视图（点击查看详情）
│   ├── 基本信息
│   ├── 状态历史
│   ├── 支付信息
│   └── 操作按钮
└── 批量操作
    ├── 批量取消
    ├── 批量退款
    └── 批量导出
```

### 2.2 核心功能

#### 2.2.1 列表视图
- **表格列**：
  - ID / 注册编号
  - 用户（姓名、邮箱）
  - 课程（名称、实例信息）
  - 状态（带颜色标识）
  - 支付状态
  - 金额
  - 注册时间
  - 操作（查看、编辑、取消）

- **筛选器**：
  - 状态筛选（cart, reserved, enrolled, waitlisted, cancelled, expired, completed）
  - 支付状态筛选（unpaid, pending, paid, refunded, failed）
  - 用户筛选（搜索用户）
  - 课程筛选（选择课程）
  - 日期范围筛选

- **统计卡片**：
  - Total Enrollments
  - Active (enrolled)
  - Pending Payment (reserved)
  - In Cart
  - Waitlisted
  - Cancelled
  - Total Revenue (paid enrollments)

#### 2.2.2 详情视图
- **基本信息**：
  - 注册 ID
  - 用户信息（姓名、邮箱、电话）
  - 课程信息（名称、实例、地点、时间）
  - 当前状态
  - 支付信息（状态、金额、交易 ID）

- **状态历史**：
  - 时间线显示所有状态变更
  - 变更原因
  - 操作人

- **操作按钮**：
  - 修改状态（需要选择新状态和原因）
  - 取消注册
  - 处理退款
  - 发送通知

#### 2.2.3 批量操作
- **批量取消**：选择多个注册，批量取消
- **批量退款**：选择已支付的注册，批量退款
- **批量导出**：导出筛选后的数据为 CSV

### 2.3 权限控制

- 只有 `admin` 角色可以访问
- 所有操作记录在 `enrollment_status_history` 中
- 敏感操作（如退款）需要确认

---

## 3. 技术实现

### 3.1 API 路由

#### `GET /api/admin/enrollments`
- 获取所有注册（支持筛选、分页、排序）
- 查询参数：
  - `status`: 状态筛选
  - `payment_status`: 支付状态筛选
  - `user_id`: 用户筛选
  - `instance_id`: 实例筛选
  - `course_id`: 课程筛选
  - `start_date`: 开始日期
  - `end_date`: 结束日期
  - `search`: 搜索关键词（用户姓名、邮箱、课程名称）
  - `page`: 页码
  - `limit`: 每页数量
  - `sort_by`: 排序字段
  - `sort_order`: 排序方向（asc/desc）

#### `GET /api/admin/enrollments/[id]`
- 获取单个注册详情（包含完整信息和历史）

#### `PATCH /api/admin/enrollments/[id]`
- 更新注册状态
- 请求体：
  ```json
  {
    "status": "cancelled",
    "payment_status": "refunded",
    "reason": "Customer request",
    "notes": "Full refund processed"
  }
  ```

#### `POST /api/admin/enrollments/batch`
- 批量操作
- 请求体：
  ```json
  {
    "action": "cancel" | "refund" | "export",
    "enrollment_ids": ["id1", "id2"],
    "reason": "Batch cancellation"
  }
  ```

#### `GET /api/admin/enrollments/stats`
- 获取统计数据
- 返回：
  ```json
  {
    "total": 100,
    "by_status": { "enrolled": 50, "cart": 10, ... },
    "by_payment_status": { "paid": 45, "pending": 5, ... },
    "revenue": { "total": 5000, "this_month": 1000, ... }
  }
  ```

### 3.2 数据库查询优化

- 使用索引优化查询性能
- 支持分页避免大量数据加载
- 使用 JOIN 优化关联查询

---

## 4. UI/UX 设计

### 4.1 列表页面
- 使用 Table 组件显示数据
- 筛选器使用 Card 组件，可折叠
- 统计卡片使用 Grid 布局
- 支持排序和分页

### 4.2 详情对话框/页面
- 使用 Dialog 或 Sheet 显示详情
- 状态历史使用时间线组件
- 操作按钮分组显示

### 4.3 响应式设计
- 移动端使用卡片布局
- 桌面端使用表格布局

---

## 5. 实施优先级

### Phase 1: 基础功能（高优先级）
1. ✅ 列表视图（所有状态）
2. ✅ 基础筛选（状态、日期范围）
3. ✅ 详情查看
4. ✅ 状态修改

### Phase 2: 增强功能（中优先级）
1. ⏳ 高级筛选（用户、课程、支付状态）
2. ⏳ 搜索功能
3. ⏳ 统计卡片
4. ⏳ 状态历史查看

### Phase 3: 高级功能（低优先级）
1. ⏳ 批量操作
2. ⏳ 数据导出
3. ⏳ 高级统计
4. ⏳ 通知功能

---

## 6. 安全考虑

1. **权限验证**：所有 API 路由验证 admin 角色
2. **操作日志**：所有状态变更记录在 `enrollment_status_history`
3. **敏感操作确认**：退款等操作需要二次确认
4. **数据脱敏**：显示用户信息时考虑隐私（可选）

---

*本文档为设计方案，实施前需要团队评审和确认。*

