# 移动端 App 首页和底部导航菜单设计方案

**版本**: 1.0  
**日期**: 2025-12  
**项目**: Blaze Robotics Academy Mobile App

---

## 1. 概述

本文档设计移动端 App 的首页布局和底部导航菜单，基于现有 Web 实现，针对移动端用户体验进行优化。

### 1.1 设计原则

- **移动优先**：针对移动端触摸交互优化
- **信息层次**：重要信息优先展示
- **快速访问**：底部导航提供核心功能快速入口
- **一致性**：与 Web 端保持品牌和功能一致性
- **原生体验**：符合 iOS/Android 设计规范

---

## 2. 首页设计

### 2.1 整体布局结构

```
┌─────────────────────────────────┐
│  Top Bar (固定顶部)              │
│  - Logo / 搜索 / 通知 / 用户头像 │
├─────────────────────────────────┤
│                                 │
│  Hero Section (简化版)           │
│  - 品牌标语                     │
│  - 主 CTA 按钮                  │
├─────────────────────────────────┤
│                                 │
│  Quick Actions (快捷操作卡片)    │
│  - 浏览课程                     │
│  - 选择地点                     │
│  - 我的课程                     │
│  - AI 客服                      │
├─────────────────────────────────┤
│                                 │
│  Featured Courses (精选课程)    │
│  - 横向滚动卡片列表             │
│  - 显示 3-5 个精选课程           │
├─────────────────────────────────┤
│                                 │
│  Location Selection (地点选择)   │
│  - 地点卡片网格（2x2）          │
│  - 显示可用地点                 │
├─────────────────────────────────┤
│                                 │
│  Programs Overview (课程体系)    │
│  - 课程分类卡片                 │
│  - 可折叠/展开                  │
├─────────────────────────────────┤
│                                 │
│  Recent Activity (最近活动)      │
│  - 我的课程进度                 │
│  - 待办事项                     │
│  (仅登录用户显示)               │
├─────────────────────────────────┤
│                                 │
│  Bottom Navigation (固定底部)    │
│  - Home / Courses / My Courses  │
│  - Locations / Profile          │
└─────────────────────────────────┘
```

### 2.2 顶部导航栏（Top Bar）

**位置**：固定在页面顶部，滚动时保持可见

**内容**：
- **左侧**：
  - Logo（点击返回首页）
  - 当前地点标签（如："Bellevue Campus"）- 可选，如果用户选择了地点

- **右侧**：
  - **搜索图标**：点击打开搜索页面
  - **通知图标**：显示未读通知数量（Badge）
  - **用户头像/登录按钮**：
    - 未登录：显示"登录"按钮
    - 已登录：显示用户头像（点击打开用户菜单）

**设计特点**：
- 高度：56px（iOS）或 64px（Android）
- 背景：白色/深色模式适配
- 阴影：轻微底部阴影，增加层次感

### 2.3 Hero Section（主视觉区域）

**位置**：顶部导航栏下方

**内容**：
- **主标题**：
  - "From Imagination To Innovation"
  - 字体：大号、加粗
  - 颜色：渐变色（品牌色）

- **副标题**：
  - "Blaze your trail with robotics"
  - 字体：中等大小
  - 颜色：次要文本色

- **主 CTA 按钮**：
  - "Browse Courses" 或 "Find Programs"
  - 点击跳转到课程目录页面
  - 样式：主要按钮（品牌色背景）

**设计特点**：
- 高度：约 200-250px
- 背景：渐变背景或品牌图片
- 布局：垂直居中，文字左对齐
- 响应式：适配不同屏幕尺寸

### 2.4 Quick Actions（快捷操作卡片）

**位置**：Hero Section 下方

**布局**：2x2 网格布局

**卡片内容**：

1. **Browse Courses（浏览课程）**
   - 图标：BookOpen
   - 标题："Browse Courses"
   - 描述："Explore all available courses"
   - 点击：跳转到 `/course-catalog`

2. **Choose Location（选择地点）**
   - 图标：MapPin
   - 标题："Choose Location"
   - 描述："Find programs near you"
   - 点击：打开地点选择页面或滚动到地点选择区域

3. **My Courses（我的课程）**
   - 图标：User
   - 标题："My Courses"
   - 描述："View your enrollments"
   - 点击：跳转到 `/profile` 的 "My Courses" 标签
   - **显示条件**：仅登录用户显示，未登录显示 "Sign In" 卡片

4. **AI Assistant（AI 客服）**
   - 图标：MessageCircle 或 Sparkles
   - 标题："Ask AI"
   - 描述："Get instant help"
   - 点击：打开 AI 客服对话框

**设计特点**：
- 卡片样式：圆角、阴影、悬停效果
- 图标：大号图标（48x48px）
- 间距：卡片之间 16px 间距
- 响应式：在小屏幕上可改为单列布局

### 2.5 Featured Courses（精选课程）

**位置**：Quick Actions 下方

**标题**：
- "Featured Courses" 或 "Popular Courses"
- 右侧显示 "View All" 链接

**内容**：
- **横向滚动列表**：
  - 显示 3-5 个精选课程卡片
  - 卡片内容：
    - 课程封面图片
    - 课程名称
    - 课程分类/系列
    - 价格（如果有）
    - "Enroll" 按钮
  - 支持左右滑动查看更多

**设计特点**：
- 卡片宽度：约屏幕宽度的 80-85%
- 卡片间距：16px
- 滚动指示：显示部分下一张卡片，提示可滚动
- 点击卡片：跳转到课程详情页

### 2.6 Location Selection（地点选择）

**位置**：Featured Courses 下方

**标题**：
- "Choose Your Location"
- 副标题："Select a campus near you"

**内容**：
- **地点卡片网格**：
  - 布局：2x2 或 2x3 网格（根据地点数量）
  - 每个卡片显示：
    - 地点名称（如："Bellevue Campus"）
    - 简要描述或地址
    - 地点图片（可选）
    - "View Programs" 按钮
  - 点击卡片：跳转到 `/locations/{code}`

**设计特点**：
- 卡片样式：圆角、边框、图片背景
- 响应式：根据屏幕宽度调整列数
- 如果地点较多：支持滚动查看更多

### 2.7 Programs Overview（课程体系概览）

**位置**：Location Selection 下方

**标题**：
- "Our Programs"
- 副标题："Explore our course categories"

**内容**：
- **课程分类卡片列表**：
  - 每个分类显示：
    - 分类图标
    - 分类名称（如："RoboQuests"、"LaunchPad"、"RoboChamps"）
    - 简要描述
    - 适龄范围
    - 箭头图标（表示可展开）
  - 点击卡片：展开显示该分类下的子分类或跳转到分类详情页
  - 支持折叠/展开

**设计特点**：
- 列表样式：垂直列表，每个卡片占一行
- 交互：点击展开/折叠，显示子分类
- 如果内容较多：支持"查看更多"按钮

### 2.8 Recent Activity（最近活动）- 可选

**位置**：Programs Overview 下方

**显示条件**：仅登录用户显示

**内容**：
- **我的课程进度**：
  - 显示最近学习的课程
  - 显示学习进度条
  - "Continue Learning" 按钮

- **待办事项**：
  - 待支付的订单
  - 待确认的报名
  - 课程提醒

**设计特点**：
- 卡片样式：信息卡片
- 交互：点击跳转到相应页面
- 如果无活动：显示空状态提示

---

## 3. 底部导航菜单设计

### 3.1 整体设计

**位置**：固定在屏幕底部

**高度**：
- iOS：约 83px（包含安全区域）
- Android：约 64px

**设计特点**：
- 背景：白色/深色模式适配
- 顶部边框：轻微分隔线
- 图标：未选中状态（灰色），选中状态（品牌色）
- 文字：小号字体，显示在图标下方

### 3.2 导航菜单项

#### 3.2.1 Home（首页）

**图标**：Home
**标签**："Home"
**功能**：
- 点击跳转到首页（`/`）
- 显示首页内容（如上述设计）
- 选中状态：当前在首页时高亮显示

**使用场景**：
- 用户想要返回首页
- 查看最新内容和推荐

---

#### 3.2.2 Courses（课程）

**图标**：BookOpen 或 GraduationCap
**标签**："Courses"
**功能**：
- 点击跳转到课程目录页面（`/course-catalog`）
- 显示所有可用课程列表
- 支持筛选和搜索
- 选中状态：当前在课程相关页面时高亮显示

**使用场景**：
- 用户想要浏览所有课程
- 搜索特定课程
- 按分类筛选课程

**子功能**（可选，通过长按或右上角菜单）：
- 按分类浏览
- 按地点筛选
- 按年龄筛选
- 查看学习路径

---

#### 3.2.3 My Courses（我的课程）

**图标**：User 或 BookMark
**标签**："My Courses"
**功能**：
- **未登录用户**：
  - 点击跳转到登录页面
  - 或显示提示："Please sign in to view your courses"
  
- **已登录用户**：
  - 点击跳转到用户门户的"我的课程"标签（`/profile`，默认显示 "My Courses" 标签）
  - 显示：
    - 已报名的课程列表
    - 课程进度
    - 待办事项（如：待支付、待确认）
    - 购物车中的课程
    - 等待列表中的课程

**使用场景**：
- 查看已报名的课程
- 查看课程进度
- 管理购物车
- 查看待办事项

**Badge 显示**：
- 如果有待支付的订单：显示红色 Badge（数字）
- 如果有购物车中的课程：显示 Badge（数字）

---

#### 3.2.4 Locations（地点）

**图标**：MapPin
**标签**："Locations"
**功能**：
- 点击跳转到地点列表页面或打开地点选择界面
- 显示所有可用地点（Franchise）
- 每个地点显示：
  - 地点名称
  - 地址
  - 可用课程数量
  - 地点图片（可选）
- 支持地图视图（可选）
- 支持搜索地点

**使用场景**：
- 用户想要选择学习地点
- 查看不同地点的课程
- 查找附近的地点

**选中状态**：
- 如果用户选择了地点：显示选中地点的名称或图标
- 当前在地点相关页面时高亮显示

---

#### 3.2.5 Profile（个人中心）

**图标**：User
**标签**："Profile"
**功能**：
- **未登录用户**：
  - 点击跳转到登录页面
  - 图标显示为默认用户图标
  
- **已登录用户**：
  - 点击跳转到用户门户（`/profile`）
  - 显示用户头像（如果有）
  - 用户门户包含：
    - 个人资料
    - 我的课程
    - 学习统计
    - 支付方式
    - 订单历史
    - 设置

**使用场景**：
- 查看和管理个人信息
- 查看学习进度和统计
- 管理支付方式
- 查看订单历史
- 应用设置

**Badge 显示**：
- 如果有未读通知：显示红色 Badge（点或数字）

---

### 3.3 导航菜单布局

**标准布局**（5 个菜单项）：

```
┌──────┬──────┬──────┬──────┬──────┐
│ Home │Courses│ My   │Loca- │Pro-  │
│      │      │Courses│tions │file  │
│  🏠  │  📚  │  👤  │  📍  │  ⚙️  │
└──────┴──────┴──────┴──────┴──────┘
```

**可选布局**（4 个菜单项，如果 My Courses 合并到 Profile）：

```
┌──────┬──────┬──────┬──────┐
│ Home │Courses│Loca- │Pro-  │
│      │      │tions │file  │
│  🏠  │  📚  │  📍  │  👤  │
└──────┴──────┴──────┴──────┘
```

**推荐布局**：5 个菜单项，因为：
- My Courses 是核心功能，需要快速访问
- 与 Profile 分离，功能更清晰
- 符合移动端应用常见模式

---

## 4. 交互设计

### 4.1 导航切换

**切换动画**：
- iOS：淡入淡出或滑动
- Android：滑动或淡入淡出
- 动画时长：200-300ms

**选中状态**：
- 图标：品牌色，可能稍大
- 文字：品牌色，加粗
- 背景：轻微背景色（可选）

### 4.2 页面滚动

**首页滚动**：
- 支持垂直滚动
- 顶部导航栏固定
- 底部导航栏固定
- 内容区域可滚动

**滚动指示**：
- 显示滚动进度（可选）
- 支持"回到顶部"按钮（滚动一定距离后显示）

### 4.3 手势支持

**滑动操作**：
- 左右滑动切换标签（在课程列表等页面）
- 下拉刷新（在列表页面）

**长按操作**：
- 长按导航图标：显示快捷菜单（可选）

---

## 5. 响应式设计

### 5.1 屏幕尺寸适配

**小屏幕**（< 375px）：
- 减少卡片间距
- 简化文字描述
- 图标和文字可能更紧凑

**中等屏幕**（375px - 414px）：
- 标准布局
- 2x2 网格布局

**大屏幕**（> 414px）：
- 可以显示更多内容
- 3x2 或更多网格布局
- 更大的卡片尺寸

### 5.2 横屏适配

**横屏模式**：
- 底部导航栏可能隐藏或改为侧边栏
- 内容布局调整为横向
- 保持核心功能可访问

---

## 6. 状态管理

### 6.1 登录状态

**未登录状态**：
- "My Courses" 显示为灰色或禁用
- "Profile" 显示默认图标
- 点击时提示登录或跳转到登录页

**已登录状态**：
- 所有功能可用
- 显示用户相关信息
- 显示 Badge 和通知

### 6.2 选中状态

**当前页面指示**：
- 底部导航栏高亮当前页面
- 顶部导航栏显示页面标题
- 支持深层链接（Deep Linking）

---

## 7. 性能优化

### 7.1 加载策略

**首页加载**：
- 优先加载 Hero Section 和 Quick Actions
- 延迟加载 Featured Courses 和 Programs Overview
- 使用骨架屏（Skeleton Screen）提升体验

**图片优化**：
- 使用 WebP 格式
- 懒加载图片
- 响应式图片尺寸

### 7.2 缓存策略

**数据缓存**：
- 缓存课程列表
- 缓存地点信息
- 缓存用户数据

---

## 8. 无障碍设计

### 8.1 可访问性

**屏幕阅读器支持**：
- 所有图标和按钮都有文字标签
- 使用语义化 HTML
- 支持 VoiceOver（iOS）和 TalkBack（Android）

**对比度**：
- 文字和背景对比度符合 WCAG AA 标准
- 图标清晰可见

**触摸目标**：
- 所有可点击元素至少 44x44px（iOS）或 48x48px（Android）
- 足够的间距避免误触

---

## 9. 设计规范

### 9.1 颜色

**品牌色**：
- 主色：渐变色（粉色到紫色，蓝色渐变）
- 辅助色：根据设计系统

**状态色**：
- 成功：绿色
- 警告：黄色/橙色
- 错误：红色
- 信息：蓝色

### 9.2 字体

**字体大小**：
- 标题：20-24px
- 正文：14-16px
- 辅助文字：12-14px
- 按钮文字：16px

**字体粗细**：
- 标题：Bold（700）
- 正文：Regular（400）
- 强调：Medium（500）

### 9.3 间距

**标准间距**：
- 卡片间距：16px
- 内容边距：16-20px
- 区块间距：24-32px

---

## 10. 实施建议

### 10.1 开发优先级

**Phase 1（MVP）**：
1. 底部导航栏（5 个菜单项）
2. 首页基础布局（Hero、Quick Actions、Featured Courses）
3. 基本页面跳转

**Phase 2**：
1. 地点选择功能
2. 课程体系概览
3. 用户状态管理

**Phase 3**：
1. 高级功能（搜索、筛选）
2. 个性化推荐
3. 性能优化

### 10.2 技术实现

**组件结构**：
- `AppBottomNavigation`：底部导航组件
- `HomePage`：首页组件
- `QuickActions`：快捷操作卡片组件
- `FeaturedCourses`：精选课程组件
- `LocationSelection`：地点选择组件

**状态管理**：
- 使用 React Context 或状态管理库
- 管理当前选中的导航项
- 管理用户登录状态

**路由管理**：
- 使用 Next.js 路由
- 支持深层链接
- 处理导航状态同步

---

## 11. 总结

### 11.1 核心功能

**首页**：
- 品牌展示（Hero Section）
- 快速操作入口（Quick Actions）
- 精选内容展示（Featured Courses）
- 地点选择（Location Selection）
- 课程体系概览（Programs Overview）

**底部导航**：
- Home：首页
- Courses：课程目录
- My Courses：我的课程（登录用户）
- Locations：地点选择
- Profile：个人中心

### 11.2 设计优势

- ✅ **移动优先**：针对移动端优化
- ✅ **快速访问**：核心功能一键直达
- ✅ **信息层次**：重要信息优先展示
- ✅ **一致性**：与 Web 端保持品牌一致性
- ✅ **原生体验**：符合平台设计规范

---

**文档版本**: 1.0  
**最后更新**: 2025-12  
**维护者**: 开发团队

